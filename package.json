{
  "name": "poison-web",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w poison-core",
    "build:browser": "webpack",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "ts-loader": "^9.5.2",
    "typescript": "^5.0.0",
    "webpack": "^5.101.0",
    "webpack-cli": "^6.0.1"
  }
}
